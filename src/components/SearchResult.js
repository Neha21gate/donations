import React from 'react'

const SearchResult=(props) =>{                           //props use for passing anything

    const charity = `${props.name}`;
    return (
        <div src={{charity}}>
          
        </div>
    )
}

export default SearchResult
